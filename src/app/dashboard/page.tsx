"use client";

import { useRouter } from "next/navigation";
import { useState, useEffect } from "react";

function page() {
    const router = useRouter();

    return (
        <div className="max-container padding-container w-full h-screen"></div>
    );
}

export default page;
